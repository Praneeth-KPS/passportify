
import React from "react";

const Header = () => {
    return (
        <header className="text-white px-4 flex items-center space-x-3 mt-10">
            <img src="/logo-transparent.png" alt="Logo" width={260} height={50} />
        </header>
    );
};

export default Header;
